<%= form_with(model: @purchased_product, local: true) do |f| %>

  <%= f.hidden_field :products_id, value: @product.id %>

  <div class="form-group">
    <%= f.label :coupons_id %>

    <%= f.collection_select :coupons_id, Coupon.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :customers_id %>

    <%= f.collection_select :customers_id, Customer.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :quantity %>

    <%= f.text_field :quantity, class: "form-control" %>
  </div>

  <%= f.submit "Create purchased product", class: "btn btn-block btn-outline-secondary" %>
<% end %>
